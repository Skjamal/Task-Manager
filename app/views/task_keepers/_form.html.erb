<%= form_for(@task_keeper) do |f| %>
  <% if @task_keeper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_keeper.errors.count, "error") %> prohibited this task_keeper from being saved:</h2>

      <ul>
      <% @task_keeper.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
